# Vercel sets these automatically once you attach storage to the project
# (see README "Deploy" section) — you don't need to fill these in by hand
# unless you're running locally against your own Postgres instance.

DATABASE_URL=
BLOB_READ_WRITE_TOKEN=
